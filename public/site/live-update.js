(function(){
  document.head.insertAdjacentHTML('beforeend','<link rel="stylesheet" href="live-update.css"><link rel="stylesheet" href="fixture-detail.css"><link rel="stylesheet" href="fixture-table.css"><link rel="stylesheet" href="fixture-readable.css"><link rel="stylesheet" href="fixture-results.css"><link rel="stylesheet" href="fixture-stats.css"><link rel="stylesheet" href="fixture-stats-readable.css"><link rel="stylesheet" href="modal-standings.css"><link rel="stylesheet" href="modal-standings-large.css"><link rel="stylesheet" href="fixture-score-status.css"><link rel="stylesheet" href="fixture-home-icon.css"><link rel="stylesheet" href="fixture-alignment.css"><link rel="stylesheet" href="fixture-goal-splits.css">');
  const nav=document.querySelector('nav'),main=document.querySelector('main'),season=document.querySelector('.season');
  const scheduleBtn=document.createElement('button');scheduleBtn.textContent='赛程安排';scheduleBtn.id='scheduleBtn';nav.appendChild(scheduleBtn);
  season.insertAdjacentHTML('beforebegin','<div class="update-wrap"><span class="updated-at" id="updatedAt">静态数据 · 07-12</span><button class="update-btn" id="updateBtn"><span class="refresh-icon">↻</span><span class="label">更新数据</span></button></div>');
  main.insertAdjacentHTML('beforeend','<section class="schedule-section" id="schedulePage"><div class="section-head"><div><p class="eyebrow">UPCOMING FIXTURES</p><h2>未来赛程</h2></div><span id="fixtureCount">正在获取…</span></div><div class="schedule-grid" id="scheduleGrid"><div class="empty-schedule">正在载入最新赛程…</div></div></section>');
  document.body.insertAdjacentHTML('beforeend','<div class="data-toast" id="dataToast"></div>');
  document.body.insertAdjacentHTML('beforeend','<aside class="modal-standings" id="modalStandings"></aside>');
  let upcoming=[],allUpcoming=[];
  const canonical=n=>aliases[n]||n;
  const toast=msg=>{const el=document.querySelector('#dataToast');el.textContent=msg;el.classList.add('show');setTimeout(()=>el.classList.remove('show'),2500)};
  const rankColor=rank=>{const hue=12+(rank-1)*(208/15);return `hsl(${hue} 72% ${rank<5?48:43}%)`};
  const recalc=()=>{
    const stat=new Map(teams.map(t=>[t[1],{p:0,w:0,d:0,l:0,gf:0,ga:0,form:[]}]));
    [...matches].reverse().forEach(m=>{const [hg,ag]=m[3].split('-').map(Number),h=stat.get(m[1]),a=stat.get(m[2]);if(!h||!a)return;h.p++;a.p++;h.gf+=hg;h.ga+=ag;a.gf+=ag;a.ga+=hg;const hr=hg>ag?'W':hg===ag?'D':'L',ar=hg<ag?'W':hg===ag?'D':'L';h[hr.toLowerCase()]++;a[ar.toLowerCase()]++;h.form.push(hr);a.form.push(ar)});
    teams.forEach(t=>{const s=stat.get(t[1]);t[3]=s.p;t[4]=s.w;t[5]=s.d;t[6]=s.l;t[7]=s.gf;t[8]=s.ga;t[9]=s.w*3+s.d;t[10]=s.form.slice(-5).join('')});
    teams.sort((a,b)=>b[9]-a[9]||(b[7]-b[8])-(a[7]-a[8])||b[7]-a[7]);render(document.querySelector('#search').value);
    document.querySelector('.hero-stat b').textContent=matches.length;
  };
  const renderSchedule=()=>{
    const grid=document.querySelector('#scheduleGrid'),count=document.querySelector('#fixtureCount');
    count.textContent=`${upcoming.length} 场待赛`;
    if(!upcoming.length){grid.innerHTML='<div class="empty-schedule">当前没有获取到未来赛程，请点击“更新数据”重试。</div>';return}
    const days={};upcoming.forEach(f=>(days[f.date]??=[]).push(f));
    grid.innerHTML=Object.entries(days).slice(0,9).map(([date,fs])=>`<article class="fixture-day"><div class="fixture-date"><span>${date.slice(5).replace('-','月')}日</span><b>${['日','一','二','三','四','五','六'][new Date(date+'T12:00:00').getDay()]}</b></div>${fs.map(f=>`<div class="fixture" data-home="${encodeURIComponent(f.home)}" data-away="${encodeURIComponent(f.away)}"><time>${f.time} · 挪超</time><div class="fixture-teams"><span>${display(f.home)}</span><i>VS</i><span>${display(f.away)}</span></div><small class="fixture-more">查看比赛详情 →</small></div>`).join('')}</article>`).join('');
    grid.querySelectorAll('.fixture').forEach(el=>el.onclick=()=>openFixtureDetail(decodeURIComponent(el.dataset.home),decodeURIComponent(el.dataset.away)));
  };
  const openTeamLive=name=>{
    const t=teams.find(x=>x[1]===name),games=matches.filter(m=>m[1]===name||m[2]===name),rank=teams.indexOf(t)+1;
    document.querySelector('#drawerContent').innerHTML=`<div class="team-hero"><div class="bigbadge">${t[2]}</div><p class="eyebrow">2026 SEASON · 第 ${rank} 名</p><h2>${t[0]}<small>（${t[1]}）</small></h2><span>Eliteserien · Norway</span></div><div class="summary"><div><b>${t[9]}</b><span>积分</span></div><div><b>${t[4]}</b><span>胜</span></div><div><b>${t[5]}</b><span>平</span></div><div><b>${t[6]}</b><span>负</span></div></div><div class="history"><div class="history-title"><h3>本赛季全部比赛</h3><span>共 ${games.length} 场</span></div><div class="history-label"><span>日期</span><span>对手（当前排名）</span><span>半场</span><span>全场</span></div>${games.map(m=>{let home=m[1]===name,sc=m[3].split('-').map(Number),a=home?sc[0]:sc[1],b=home?sc[1]:sc[0],o=a>b?'W':a===b?'D':'L',opp=home?m[2]:m[1],ot=teams.find(x=>x[1]===opp),orank=ot?teams.indexOf(ot)+1:'–';return `<div class="game"><span>${m[0].slice(5)}</span><span class="opponent"><i class="outcome ${o.toLowerCase()}">${o==='W'?'胜':o==='D'?'平':'负'}</i><i class="rank-chip" style="background:${rankColor(orank)}" title="当前第 ${orank} 名">${orank}</i><span class="opponent-name">${home?'主':'客'} · ${display(opp)}</span></span><span class="ht">${m[4]}</span><span class="score">${m[3]}</span></div>`}).join('')}</div><p class="source">排名徽章：暖色代表积分榜前列，冷色代表后列；排名随数据更新同步变化。<br>数据源：ESPN / Eliteserien，半场比分由进球时间计算。</p>`;
    drawer.classList.add('open');overlay.classList.add('open');
  };
  window.openTeam=openTeamLive;
  const shortName=n=>{const t=teams.find(x=>x[1]===n);return t?t[0]:n};
  const fixtureName=(h,a)=>`<span class="fixture-name" title="${h} vs ${a}"><b>${shortName(h)}</b><i>VS</i><b>${shortName(a)}</b></span>`;
  const tableHead=(future=false)=>`<div class="detail-table-head ${future?'future-head':''}"><span>日期</span><span>比赛</span>${future?'<span>时间</span>':'<span>半场</span><span>全场</span>'}</div>`;
  const scoreRow=m=>`<div class="detail-row"><time>${m[0]}</time><span class="clubs">${fixtureName(m[1],m[2])}</span><span class="muted-score">${m[4]}</span><strong>${m[3]}</strong></div>`;
  const futureRows=(team)=>{const fs=allUpcoming.filter(f=>f.home===team||f.away===team);return tableHead(true)+(fs.length?fs.map(f=>`<div class="detail-row future-row"><time>${f.date}</time><span class="clubs">${fixtureName(f.home,f.away)}</span><strong>${f.time}</strong></div>`).join(''):'<div class="detail-row future-row"><time>—</time><span class="clubs">暂无已公布赛程</span><strong>—</strong></div>')};
  async function fetchH2H(home,away){
    const years=[2023,2024,2025];let found=[];
    const payloads=await Promise.all(years.map(y=>fetch(`https://site.api.espn.com/apis/site/v2/sports/soccer/nor.1/scoreboard?dates=${y}&limit=300`).then(r=>r.ok?r.json():{events:[]}).catch(()=>({events:[]}))));
    payloads.forEach(data=>(data.events||[]).forEach(e=>{if(!e.status?.type?.completed)return;const c=e.competitions[0],h=c.competitors.find(x=>x.homeAway==='home'),a=c.competitors.find(x=>x.homeAway==='away'),hn=canonical(h.team.displayName),an=canonical(a.team.displayName);if(!((hn===home&&an===away)||(hn===away&&an===home)))return;let hh=0,ha=0;(c.details||[]).filter(x=>x.scoringPlay&&Number(x.clock.value)<=2700).forEach(x=>x.team.id===h.id?hh++:ha++);found.push([e.date.slice(0,10),hn,an,h.score+'-'+a.score,hh+'-'+ha])}));
    const current=matches.filter(m=>(m[1]===home&&m[2]===away)||(m[1]===away&&m[2]===home));return [...found,...current].filter((m,i,a)=>a.findIndex(x=>x[0]===m[0]&&x[1]===m[1]&&x[2]===m[2])===i).sort((a,b)=>b[0].localeCompare(a[0])).slice(0,12);
  }
  const calculateStats=(games,team)=>{const s={n:games.length,ft:{w:0,d:0,l:0},ht:{w:0,d:0,l:0},goals:0};games.forEach(m=>{const isHome=m[1]===team,[fh,fa]=m[3].split('-').map(Number),[hh,ha]=m[4].split('-').map(Number),fg=isHome?fh:fa,fo=isHome?fa:fh,hg=isHome?hh:ha,ho=isHome?ha:hh;s.goals+=fg;s.ft[fg>fo?'w':fg<fo?'l':'d']++;s.ht[hg>ho?'w':hg<ho?'l':'d']++});return s};
  const pct=(v,n)=>n?Math.round(v/n*100):0;
  const statsCard=(team,games,scope)=>{const s=calculateStats(games,team),row=(label,x)=>`<div class="stat-line"><span>${label}</span><b class="stat-w">胜 ${x.w}<small>${pct(x.w,s.n)}%</small></b><b class="stat-d">平 ${x.d}<small>${pct(x.d,s.n)}%</small></b><b class="stat-l">负 ${x.l}<small>${pct(x.l,s.n)}%</small></b></div>`;return `<article class="record-stat"><header><strong>${shortName(team)}</strong><span>${scope} · ${s.n} 场</span></header>${row('全场',s.ft)}${row('半场',s.ht)}<footer><span>场均进球</span><strong>${s.n?(s.goals/s.n).toFixed(2):'0.00'}</strong></footer></article>`};
  const renderSideStandings=(home,away)=>{const panel=document.querySelector('#modalStandings');panel.innerHTML=`<div class="side-table-title"><div><p>ELITESERIEN · 2026</p><h2>实时积分榜</h2></div><span>共 16 队</span></div><div class="side-table-head"><span>#</span><span>球队</span><span>赛</span><span>净胜</span><span>积分</span></div><div class="side-table-body">${teams.map((t,i)=>`<div class="side-team ${t[1]===home||t[1]===away?'selected':''} zone-${i+1}"><b>${i+1}</b><span><strong>${t[0]}</strong><small>${t[1]}</small></span><i>${t[3]}</i><i>${t[7]-t[8]>0?'+':''}${t[7]-t[8]}</i><em>${t[9]}</em></div>`).join('')}</div><div class="side-legend"><span><i></i>直通欧战</span><span><i></i>附加赛</span><span><i></i>降级</span></div>`;panel.classList.add('open')};
  async function openFixtureDetail(home,away){
    renderSideStandings(home,away);
    drawer.classList.add('open');overlay.classList.add('open');document.querySelector('#drawerContent').innerHTML=`<div class="match-detail-head"><p class="eyebrow">MATCH INTELLIGENCE</p><h2>${display(home)}<br><span class="versus">VS</span><br>${display(away)}</h2><span>赛前数据中心 · 2026 挪超</span></div><div class="loading-detail"><div class="loading-ball"></div>正在整理历史交锋与赛季数据…</div>`;
    let h2h=[],failed=false;try{h2h=await fetchH2H(home,away)}catch(e){failed=true;h2h=matches.filter(m=>(m[1]===home&&m[2]===away)||(m[1]===away&&m[2]===home))}
    const homeGames=matches.filter(m=>m[1]===home||m[2]===home),awayGames=matches.filter(m=>m[1]===away||m[2]===away);
    document.querySelector('#drawerContent').innerHTML=`<div class="match-detail-head"><p class="eyebrow">MATCH INTELLIGENCE</p><h2>${display(home)}<br><span class="versus">VS</span><br>${display(away)}</h2><span>赛前数据中心 · 2026 挪超</span></div><div class="detail-tabs"><button class="active" data-jump="h2h">历史交锋</button><button data-jump="seasonForm">赛季战绩</button><button data-jump="futureGames">未来赛程</button></div>${failed?'<div class="detail-error">历史数据源连接失败，当前仅展示本赛季本地数据。</div>':''}<section class="detail-section" id="h2h"><h3>历史交手记录</h3><p>近期最多 12 次联赛交锋</p><div class="detail-list">${tableHead()}${h2h.length?h2h.map(m=>scoreRow(m)).join(''):'<div class="detail-row"><time>—</time><span class="clubs">暂无可用交锋记录</span><span>—</span><strong>—</strong></div>'}</div></section><section class="detail-section" id="seasonForm"><h3>本赛季历史战绩</h3><p>两队 2026 赛季全部已完赛比赛</p><h4 class="team-divider">${display(home)} · ${homeGames.length} 场</h4><div class="detail-list">${tableHead()}${homeGames.map(m=>scoreRow(m)).join('')}</div><h4 class="team-divider">${display(away)} · ${awayGames.length} 场</h4><div class="detail-list">${tableHead()}${awayGames.map(m=>scoreRow(m)).join('')}</div></section><section class="detail-section" id="futureGames"><h3>本赛季未来比赛</h3><p>依据当前已公布的完整剩余赛程</p><h4 class="team-divider">${display(home)}</h4><div class="detail-list">${futureRows(home)}</div><h4 class="team-divider">${display(away)}</h4><div class="detail-list">${futureRows(away)}</div></section>`;
    const h2hList=document.querySelector('#h2h .detail-list');h2hList.insertAdjacentHTML('beforebegin',`<div class="record-stat-grid">${statsCard(home,h2h,'交锋')}${statsCard(away,h2h,'交锋')}</div>`);h2hList.querySelectorAll('.detail-row').forEach((row,i)=>{const m=h2h[i];if(!m)return;const [fh,fa]=m[3].split('-').map(Number),[hh,ha]=m[4].split('-').map(Number),currentWasHome=m[1]===home,us=currentWasHome?fh:fa,them=currentWasHome?fa:fh,hus=currentWasHome?hh:ha,hthem=currentWasHome?ha:hh,outcome=(a,b)=>a>b?'score-win':a<b?'score-loss':'score-draw';row.classList.add(us>them?'result-win':us<them?'result-loss':'result-draw');row.querySelector('.muted-score').classList.add(outcome(hus,hthem));row.querySelector('strong').classList.add(outcome(us,them))});
    const seasonLists=document.querySelectorAll('#seasonForm .detail-list');[[seasonLists[0],homeGames,home],[seasonLists[1],awayGames,away]].forEach(([list,games,focus])=>{if(!list)return;list.insertAdjacentHTML('beforebegin',`<div class="record-stat-grid single">${statsCard(focus,games,'本赛季')}</div>`);list.querySelectorAll('.detail-row').forEach((row,i)=>{const m=games[i],[hg,ag]=m[3].split('-').map(Number),[hh,ha]=m[4].split('-').map(Number),isHome=m[1]===focus,us=isHome?hg:ag,them=isHome?ag:hg,hus=isHome?hh:ha,hthem=isHome?ha:hh,outcome=(a,b)=>a>b?'score-win':a<b?'score-loss':'score-draw';row.classList.add(us>them?'result-win':us<them?'result-loss':'result-draw');row.querySelector('.muted-score').classList.add(outcome(hus,hthem));row.querySelector('strong').classList.add(outcome(us,them));if(isHome)row.querySelector('.clubs').insertAdjacentHTML('afterbegin','<span class="home-badge" title="主场作战">⌂ 主场</span>')})});
    [[seasonLists[0],homeGames,home],[seasonLists[1],awayGames,away]].forEach(([list,games,focus])=>{if(!list)return;let total=0,first=0;games.forEach(m=>{const isHome=m[1]===focus,[fh,fa]=m[3].split('-').map(Number),[hh,ha]=m[4].split('-').map(Number);total+=isHome?fh:fa;first+=isHome?hh:ha});const avg=v=>games.length?(v/games.length).toFixed(2):'0.00',footer=list.previousElementSibling.querySelector('.record-stat footer');footer.className='goal-splits';footer.innerHTML=`<div><span>场均进球</span><strong>${avg(total)}</strong></div><div><span>上半场场均</span><strong>${avg(first)}</strong></div><div><span>下半场场均</span><strong>${avg(total-first)}</strong></div>`});
    document.querySelectorAll('.detail-tabs button').forEach(b=>b.onclick=()=>{document.querySelectorAll('.detail-tabs button').forEach(x=>x.classList.remove('active'));b.classList.add('active');document.querySelector('#'+b.dataset.jump).scrollIntoView({behavior:'smooth',block:'start'})});
  }
  new MutationObserver(()=>{if(!drawer.classList.contains('open'))document.querySelector('#modalStandings').classList.remove('open')}).observe(drawer,{attributes:true,attributeFilter:['class']});
  const bindRows=()=>tbody.querySelectorAll('tr').forEach(r=>r.onclick=()=>openTeamLive(r.dataset.team));
  const oldRender=window.render;window.render=function(q=''){oldRender(q);bindRows()};
  bindRows();
  async function updateData(silent=false){
    const btn=document.querySelector('#updateBtn');btn.classList.add('loading');btn.disabled=true;
    try{
      const res=await fetch('https://site.api.espn.com/apis/site/v2/sports/soccer/nor.1/scoreboard?dates=2026&limit=300');
      if(!res.ok)throw new Error('HTTP '+res.status);const data=await res.json(),fresh=[],future=[],now=Date.now();
      data.events.forEach(e=>{const c=e.competitions[0],h=c.competitors.find(x=>x.homeAway==='home'),a=c.competitors.find(x=>x.homeAway==='away'),date=e.date.slice(0,10);
        if(e.status.type.completed){let hh=0,ha=0;(c.details||[]).filter(x=>x.scoringPlay&&Number(x.clock.value)<=2700).forEach(x=>x.team.id===h.id?hh++:ha++);fresh.push([date,canonical(h.team.displayName),canonical(a.team.displayName),h.score+'-'+a.score,hh+'-'+ha])}
        else if(new Date(e.date).getTime()>now){future.push({date,time:new Date(e.date).toLocaleTimeString('zh-CN',{hour:'2-digit',minute:'2-digit',hour12:false}),home:canonical(h.team.displayName),away:canonical(a.team.displayName)})}
      });
      if(fresh.length){matches.splice(0,matches.length,...fresh.sort((a,b)=>b[0].localeCompare(a[0])));recalc()}
      allUpcoming=future.sort((a,b)=>(a.date+a.time).localeCompare(b.date+b.time));const horizon=Date.now()+14*24*60*60*1000;upcoming=allUpcoming.filter(f=>new Date(f.date+'T23:59:59').getTime()<=horizon);renderSchedule();
      document.querySelector('#updatedAt').textContent='更新于 '+new Date().toLocaleTimeString('zh-CN',{hour:'2-digit',minute:'2-digit'});
      if(!silent)toast(`更新成功：${matches.length} 场赛果，${upcoming.length} 场待赛`);
    }catch(err){if(!silent)toast('更新失败，已继续使用本地数据');document.querySelector('#fixtureCount').textContent='获取失败'}
    finally{btn.classList.remove('loading');btn.disabled=false}
  }
  document.querySelector('#updateBtn').onclick=()=>updateData(false);
  scheduleBtn.onclick=()=>{document.querySelectorAll('nav button').forEach(b=>b.classList.remove('active'));scheduleBtn.classList.add('active');document.querySelector('.hero').style.display='none';document.querySelector('.layout').style.display='none';document.querySelector('.results').style.display='none';document.querySelector('#schedulePage').classList.add('active');window.scrollTo({top:0,behavior:'smooth'})};
  document.querySelectorAll('nav button:not(#scheduleBtn)').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('nav button').forEach(x=>x.classList.remove('active'));b.classList.add('active');document.querySelector('.hero').style.display='flex';document.querySelector('.layout').style.display='grid';document.querySelector('.results').style.display='block';document.querySelector('#schedulePage').classList.remove('active')}));
  updateData(true);
})(); 
